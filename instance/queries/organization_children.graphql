query organization_children($id: ID) {
	members: model(type: "member", filter: {_parent: $id}) {
		results {
			_slug
			owner: _owner_record {

			}
		}
	}
	products: model(type: "product", filter: {_parent: $id}) {
		results {
			_slug
			name
		}
	}
}
